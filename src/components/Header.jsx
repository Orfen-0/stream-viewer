import React from 'react';

const Header = () => {
    return (
        <header className="header">
            <h2>ADS Stream Viewer</h2>
            {/* Later add a settings button to open a modal/drawer */}
        </header>
    );
};

export default Header;
